---
- name: configure monolith sudoers file
  import_tasks: monolithic_sudoers.yml
  when:
    - ansible_lsb.major_release|int < 20
  tags: sudoers

- name: configure  sudoers file
  import_tasks: sudoers.yml
  when:
    - ansible_lsb.major_release|int >= 20
  tags: sudoers
